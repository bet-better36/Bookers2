 <div class="container">
  <div class="row">
    <div class="col-xs-3">
<%= render "render/uif", user:@user, book:@bookn %>
    </div>
<div class="col-xs-9">

   <h2>here is <%= @user.name %>s'_Books</h2>
<table class="table table-inverse">
  <thead>
    <tr>
      <th></th>
      <th>Title</th>
      <th>Opinion</th>
    </tr>
  </thead>

  	
    <tbody>
     <% @books.each do |book| %>
      <tr>
       <td>
        <%= link_to user_path(@user.id) do %>
        <%= attachment_image_tag @user, :profile_image, size: "40x40",  fallback: "no_image.jpg"%></td>
        <% end %>
       <td><%= link_to book.title, book_path(book.id) %></td>
       <td><%= book.body %></td>
        <td><%= link_to "Destroy", user_path(@user.id), method: :delete, class: "btn-sm btn-danger", "data-confirm" => "本当に削除しますか？" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<td><%= link_to "User Destroy", user_path(@user.id), method: :delete, class: "btn-sm btn-danger", "data-confirm" => "本当に削除しますか？" %></td>
    </div>

   </div>
  </div>

